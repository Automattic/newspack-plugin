/**
 * Expose some functionality to pages outside the My Account dashboard so we
 * can use it with Nespack UI features like notices.
 */

/**
 * Internal dependencies.
 */
import './modal-checkout';
