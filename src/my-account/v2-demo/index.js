/**
 * Newspack My Account v2 prototype demo.
 *
 * Phase 1 scaffold: imports the public-path bootstrap (required for any
 * standalone webpack entry) and the demo's scoped stylesheet. Later phases
 * wire up newsletters/donations/subscriptions interactions here.
 */

import '../../shared/js/public-path';
import './style.scss';

// eslint-disable-next-line no-console
console.log( 'Newspack My Account v2 demo bundle loaded.' );
